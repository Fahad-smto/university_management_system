import { z } from 'zod';

// TODO: define Admin zod validation schemas
export const AdminValidation = {
  // createZod: z.object({
  //   body: z.object({}),
  // }),
  // updateZod: z.object({
  //   body: z.object({}),
  // }),
};
