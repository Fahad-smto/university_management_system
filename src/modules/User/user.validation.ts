import { z } from 'zod';

// TODO: define User zod validation schemas
export const UserValidation = {
  // createZod: z.object({
  //   body: z.object({}),
  // }),
  // updateZod: z.object({
  //   body: z.object({}),
  // }),
};
