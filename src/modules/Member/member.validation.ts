import { z } from "zod";

// TODO: define Member zod validation schemas
export const MemberValidation = {
	// createZod: z.object({
	//   body: z.object({}),
	// }),
	// updateZod: z.object({
	//   body: z.object({}),
	// }),
};
