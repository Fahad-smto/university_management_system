import { z } from 'zod';

// Moderator (panel) + ModeratorMember zod schemas together here

export const moderatorValidation = {
  // createModeratorZod: z.object({ body: z.object({}) }),
  // updateModeratorZod: z.object({ body: z.object({}) }),
  // addMemberZod: z.object({ body: z.object({}) }),
};
