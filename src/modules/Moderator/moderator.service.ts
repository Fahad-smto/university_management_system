// import prisma from '../../lib/prisma';
// import { IModeratorFilters } from './moderator.interface';
// import { IPaginationOptions } from '../../interface/common';

// Moderator (panel) + ModeratorMember business logic together here

export const moderatorService = {
	// ---- Moderator ----
	// createModerator: async (payload: any) => {},
	// getAllModerators: async (filters: IModeratorFilters, options: IPaginationOptions) => {},
	// getSingleModerator: async (id: string) => {},
	// updateModerator: async (id: string, payload: any) => {},
	// softDeleteModerator: async (id: string) => {},
	// ---- ModeratorMember ----
	// addMember: async (moderatorId: string, payload: any) => {
	//   // TODO: enforce one person per position per panel (unique constraint in schema)
	// },
	// getMembers: async (moderatorId: string) => {},
	// updateMember: async (memberId: string, payload: any) => {},
	// removeMember: async (memberId: string) => {},
};
