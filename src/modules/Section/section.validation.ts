import { z } from 'zod';

// TODO: define Section zod validation schemas
export const SectionValidation = {
  // createZod: z.object({
  //   body: z.object({}),
  // }),
  // updateZod: z.object({
  //   body: z.object({}),
  // }),
};
