// import prisma from '../../lib/prisma';
// import { ISectionFilters } from './section.interface';
// import { IPaginationOptions } from '../../interface/common';

// TODO: implement Section business logic / Prisma queries here
export const SectionService = {
  // create: async (payload: any) => {},
  // getAll: async (filters: ISectionFilters, options: IPaginationOptions) => {},
  // getSingle: async (id: string) => {},
  // update: async (id: string, payload: any) => {},
  // softDelete: async (id: string) => {},
};
