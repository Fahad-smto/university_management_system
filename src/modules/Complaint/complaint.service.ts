// import prisma from '../../lib/prisma';
// import { IComplaintFilters } from './complaint.interface';
// import { IPaginationOptions } from '../../interface/common';

// TODO: implement Complaint business logic / Prisma queries here
export const ComplaintService = {
	// create: async (payload: any) => {},
	// getAll: async (filters: IComplaintFilters, options: IPaginationOptions) => {},
	// getSingle: async (id: string) => {},
	// update: async (id: string, payload: any) => {},
	// softDelete: async (id: string) => {},
};
