import { z } from "zod";

// TODO: define Complaint zod validation schemas
export const ComplaintValidation = {
	// createZod: z.object({
	//   body: z.object({}),
	// }),
	// updateZod: z.object({
	//   body: z.object({}),
	// }),
};
