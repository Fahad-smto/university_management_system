import { z } from 'zod';

// TODO: define Auth zod validation schemas
export const AuthValidation = {
  // createZod: z.object({
  //   body: z.object({}),
  // }),
  // updateZod: z.object({
  //   body: z.object({}),
  // }),
};
