import { z } from 'zod';

// TODO: define Notice zod validation schemas
export const NoticeValidation = {
  // createZod: z.object({
  //   body: z.object({}),
  // }),
  // updateZod: z.object({
  //   body: z.object({}),
  // }),
};
