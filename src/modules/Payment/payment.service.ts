// import prisma from '../../lib/prisma';
// import { IPaymentFilters } from './payment.interface';
// import { IPaginationOptions } from '../../interface/common';

// TODO: implement Payment business logic / Prisma queries here
export const PaymentService = {
  // create: async (payload: any) => {},
  // getAll: async (filters: IPaymentFilters, options: IPaginationOptions) => {},
  // getSingle: async (id: string) => {},
  // update: async (id: string, payload: any) => {},
  // softDelete: async (id: string) => {},
};
