import { z } from "zod";

// TODO: define Payment zod validation schemas
export const PaymentValidation = {
	// createZod: z.object({
	//   body: z.object({}),
	// }),
	// updateZod: z.object({
	//   body: z.object({}),
	// }),
};
