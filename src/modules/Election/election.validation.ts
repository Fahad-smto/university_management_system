import { z } from 'zod';

// ElectionInfo + ElectionCandidate zod schemas together here

export const electionValidation = {
  // createElectionZod: z.object({ body: z.object({}) }),
  // updateElectionZod: z.object({ body: z.object({}) }),
  // createCandidateZod: z.object({ body: z.object({}) }),
};
