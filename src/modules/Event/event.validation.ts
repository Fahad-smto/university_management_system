import { z } from 'zod';

// Event + EventRegistration zod schemas together here

export const eventValidation = {
  // createEventZod: z.object({ body: z.object({}) }),
  // updateEventZod: z.object({ body: z.object({}) }),
};
