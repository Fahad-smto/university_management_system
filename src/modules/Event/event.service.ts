// import prisma from '../../lib/prisma';
// import { IEventFilters } from './event.interface';
// import { IPaginationOptions } from '../../interface/common';

// Event + EventRegistration business logic together here

export const eventService = {
	// ---- Event ----
	// createEvent: async (payload: any) => {},
	// getAllEvents: async (filters: IEventFilters, options: IPaginationOptions) => {},
	// getSingleEvent: async (id: string) => {},
	// updateEvent: async (id: string, payload: any) => {},
	// softDeleteEvent: async (id: string) => {},
	// ---- EventRegistration ----
	// registerForEvent: async (eventId: string, userId: string) => {
	//   // TODO: use a transaction if the event has a seat limit,
	//   // to avoid double-booking under concurrent requests
	// },
	// getRegistrations: async (eventId: string) => {},
	// cancelRegistration: async (eventId: string, userId: string) => {},
};
