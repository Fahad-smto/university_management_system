import { z } from 'zod';

// TODO: define MonthlyDue zod validation schemas
export const MonthlyDueValidation = {
  // createZod: z.object({
  //   body: z.object({}),
  // }),
  // updateZod: z.object({
  //   body: z.object({}),
  // }),
};
